package com.gn.practice;

public class Practice11 {
	public static void main(String[] args) {
		int count = 1;
		// count가 5보다 작거나 같을 때까지 반복
		while (count <= 5) {
		    System.out.println("반복 횟수: " + count);
		    count++; // count 값을 1씩 증가시킴
		}
		System.out.println("반복 종료!");
	}
}
